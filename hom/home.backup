{ inputs, pkgs, config, home-manager, ... }: {

  nixpkgs.config.allowUnfreePredicate = _: true;
  home = {
  sessionVariables = {
  FLAKE = "/home/sophragoz/nix/hom";
  };
  packages = with pkgs; [
   inputs.zen-browser.packages."${system}".specific
   inputs.ayugram-dekstop.packages.${system}.ayugram-desktop

   cassette
   fragments
   steam
   prismlauncher
   superTux
   lutris

   libreoffice
   okular
   obsidian
   krita

   eog
   nautilus
   file-roller
   kitty
   mpv

   pyradio
   cava
   zsh

   swayfx
   wofi
   wl-clipboard
   waybar
   kando
   seatd
   pavucontrol
  ];
  username = "sophragoz";
  homeDirectory = "/home/sophragoz";
  stateVersion = "25.05";
  };


}
